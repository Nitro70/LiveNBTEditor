<Window x:Class="LiveNBT.App.AddTagWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Add Tag" Height="210" Width="380" WindowStartupLocation="CenterOwner"
        Background="{StaticResource Bg0Brush}" Foreground="{StaticResource TextPrimaryBrush}"
        ResizeMode="NoResize">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/><RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/><RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="80"/><ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Text="Name" VerticalAlignment="Center"/>
        <TextBox x:Name="NameBox" Grid.Column="1" Margin="0,2"
                 ToolTip="Leave empty when adding to a list"/>
        <TextBlock Text="Type" Grid.Row="1" VerticalAlignment="Center"/>
        <ComboBox x:Name="TypeBox" Grid.Row="1" Grid.Column="1" Margin="0,2" SelectedIndex="2"/>
        <TextBlock Text="Value" Grid.Row="2" VerticalAlignment="Center"/>
        <TextBox x:Name="ValueBox" Grid.Row="2" Grid.Column="1" Margin="0,2"/>

        <StackPanel Grid.Row="3" Grid.ColumnSpan="2" Orientation="Horizontal"
                    HorizontalAlignment="Right" VerticalAlignment="Bottom">
            <Button Content="Add" Width="70" Margin="0,0,8,0" Click="OnAdd" IsDefault="True"/>
            <Button Content="Cancel" Width="70" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
