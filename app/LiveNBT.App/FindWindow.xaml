<Window x:Class="LiveNBT.App.FindWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Find" Height="420" Width="470" MinHeight="240" MinWidth="380"
        WindowStartupLocation="CenterOwner" ShowInTaskbar="False"
        Background="{StaticResource Bg0Brush}" Foreground="{StaticResource TextPrimaryBrush}"
        PreviewKeyDown="OnWindowKeyDown" Closing="OnClosingWindow">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/><RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="60"/><ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Text="Name" VerticalAlignment="Center"/>
        <TextBox x:Name="NameBox" Grid.Column="1" Margin="0,2" TextChanged="OnQueryChanged"/>
        <TextBlock Text="Value" Grid.Row="1" VerticalAlignment="Center"/>
        <TextBox x:Name="ValueBox" Grid.Row="1" Grid.Column="1" Margin="0,2" TextChanged="OnQueryChanged"/>

        <DockPanel Grid.Row="2" Grid.ColumnSpan="2" Margin="0,8,0,0" LastChildFill="False">
            <CheckBox x:Name="RegexCheck" Content="Regex" VerticalAlignment="Center"
                      Checked="OnQueryChanged" Unchecked="OnQueryChanged"/>
            <Button DockPanel.Dock="Right" Content="Find All" Width="72" Click="OnFindAll"/>
            <Button DockPanel.Dock="Right" Content="Find Next" Width="80" Margin="0,0,8,0"
                    Click="OnFindNext" IsDefault="True"/>
            <Button DockPanel.Dock="Right" Content="◀ Prev" Width="66" Margin="0,0,8,0" Click="OnFindPrev"/>
        </DockPanel>

        <TextBlock x:Name="StatusText" Grid.Row="3" Grid.ColumnSpan="2" Margin="0,8,0,0"
                   Foreground="{StaticResource TextSecondaryBrush}"
                   Text="Empty fields match everything. Enter = next, Shift+Enter = previous."/>

        <ListBox x:Name="ResultsList" Grid.Row="4" Grid.ColumnSpan="2" Margin="0,8,0,0"
                 FontFamily="Consolas" SelectionChanged="OnResultSelected"/>
    </Grid>
</Window>
